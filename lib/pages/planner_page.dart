
import 'package:flutter/material.dart';

class PlannerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Planlayıcı")),
      body: Center(child: Text("Planlayıcı Sayfası")),
    );
  }
}
